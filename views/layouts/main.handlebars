<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Dinning List</title>

<link rel="stylesheet" href="/stylesheets/bootstrap.css">
<link rel="stylesheet" href="/stylesheets/index.css"> 
<link rel="stylesheet" href="/stylesheets/show.css">

</head>
<body>
<header>
  <!-- navigation -->
  <nav class="navbar navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">
          <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png" width="30" height="30" class="d-inline-block align-top" alt="我的餐廳清單">
          {{#if isAuthenticated}}{{user.name}}的{{/if}}老饕清單
        </a>
        <ul class="navbar-nav me-0 mb-2 mb-lg-0">
        <li class="nav-item">
          {{#unless isAuthenticated}}
          <a class="nav-link active px-2 py-1 border border-dark rounded-1" aria-current="page"
            href="/users/login">Login</a>
          {{/unless}}
          {{#if isAuthenticated}}
          <a class="nav-link active px-2 py-1 border border-dark rounded-1" aria-current="page"
            href="/users/logout">Logout</a>
          {{/if}}
        </li>
      </ul>
    </div>
  </nav>

  <div class="banner"></div>
</header>
{{{body}}}

<script src="/javascripts/popper.js"></script>
<script src="/javascripts/bootstrap.js"></script>
</body>
</html>